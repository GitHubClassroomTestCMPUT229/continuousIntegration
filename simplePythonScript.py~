import spimgrader
import subprocess
import os

def main():
    spimgrader.main()
    files = os.listdir("./results")
    for f in files:
        f = open("./results/" + f, "r")
        print(f.read())

if __name__ == "__main__":
    main()
